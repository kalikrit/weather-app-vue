{
  "name": "weather-app-ts",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint",
    "postinstall": "node scripts/update-version.js",
    "predev": "node scripts/update-version.js",
    "prebuild": "node scripts/update-version.js",
    "version:patch": "npm version patch --no-git-tag-version && node scripts/update-version.js && git add . && git commit -m \"chore: bump version to $(node -p \"require('./package.json').version\")\" && git push",
    "version:minor": "npm version minor --no-git-tag-version && node scripts/update-version.js && git add . && git commit -m \"chore: bump version to $(node -p \"require('./package.json').version\")\" && git push",
    "version:major": "npm version major --no-git-tag-version && node scripts/update-version.js && git add . && git commit -m \"chore: bump version to $(node -p \"require('./package.json').version\")\" && git push"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "core-js": "^3.8.3",
    "element-plus": "^2.14.3",
    "vue": "^3.2.13"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.4.0",
    "@typescript-eslint/parser": "^5.4.0",
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-plugin-eslint": "~5.0.0",
    "@vue/cli-plugin-typescript": "~5.0.0",
    "@vue/cli-service": "~5.0.0",
    "@vue/eslint-config-typescript": "^9.1.0",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-plugin-vue": "^8.0.3",
    "lint-staged": "^11.1.2",
    "prettier": "^2.4.1",
    "sass": "^1.32.7",
    "sass-loader": "^12.0.0",
    "typescript": "~4.5.5"
  },
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "eslintConfig": {
    "globals": {
      "defineProps": "readonly",
      "defineEmits": "readonly",
      "defineExpose": "readonly",
      "withDefaults": "readonly"
    }
  }
}
